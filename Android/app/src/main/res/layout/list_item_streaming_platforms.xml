<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingStart="12dp"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        app:cardBackgroundColor="@color/colorSecondary"
        app:cardCornerRadius="15dp"
        android:elevation="13dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="@id/tvStreamingServiceTitle"
        app:layout_constraintEnd_toEndOf="@id/tvStreamingServiceTitle"
        android:id="@+id/cvStreamingServiceBadge"
        android:layout_width="80dp"
        android:layout_height="80dp">
        <ImageView
            android:scaleType="centerInside"
            android:layout_gravity="center"
            android:id="@+id/ivStreamingServiceIcon"
            android:src="@drawable/sample_googleplay_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
    </androidx.cardview.widget.CardView>
    <TextView
        android:gravity="center"
        android:layout_marginTop="4dp"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/cvStreamingServiceBadge"
        app:layout_constraintStart_toStartOf="@id/cvStreamingServiceBadge"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="Google Play"
        android:id="@+id/tvStreamingServiceTitle"
        android:layout_width="80dp"
        android:layout_height="30dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>